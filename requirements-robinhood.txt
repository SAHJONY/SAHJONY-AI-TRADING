# Robinhood Crypto Trading API adapter (BROKER=robinhood).
# Ed25519 request signing. Install alongside the base requirements:
#   pip install -r requirements.txt -r requirements-robinhood.txt
PyNaCl>=1.5.0
